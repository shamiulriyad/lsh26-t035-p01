services:
  - type: web
    name: loadshed-planner
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    healthCheckPath: /
    envVars:
      - key: JAVA_TOOL_OPTIONS
        value: -Xmx256m -XX:+UseSerialGC
